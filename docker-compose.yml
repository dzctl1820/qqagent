services:
  # ===== QQ 机器人 =====
  bot:
    build: .
    container_name: qqagent-bot
    restart: always
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    networks:
      - bot-net

networks:
  bot-net:
    driver: bridge
